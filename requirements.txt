# FB2D — minimal environment to reproduce the experiments.
#
# Captured from the conda env `incr_learn` on the development machine
# (Python 3.11.15, CUDA 13.0, NVIDIA RTX 4090).
#
# To recreate (e.g. with conda):
#   conda create -n fb2d python=3.11 -y
#   conda activate fb2d
#   pip install -r requirements.txt
#
# torch wheel:
#   The pinned torch==2.11.0 here was installed from the cu130 wheel index.
#   On a different CUDA version (e.g. 12.4), use the matching wheel from
#   https://download.pytorch.org/whl/torchXY/  before installing the rest:
#     pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu130
#
# Direct dependencies of FB2D scripts:
torch==2.11.0
numpy==2.4.3
matplotlib==3.10.8

# matplotlib transitive deps (pinned for reproducibility):
contourpy==1.3.3
cycler==0.12.1
fonttools==4.62.1
kiwisolver==1.5.0
pillow==12.1.1

# torch transitive deps:
filelock==3.25.2
fsspec==2026.3.0
Jinja2==3.1.6
MarkupSafe==3.0.3
networkx==3.6.1
sympy==1.14.0
typing_extensions>=4.10
